"use strict"; // treat all JS codes as newer version

// alert(3+3) // we are using nodejs, not browser

console.log(3 + 3);
console.log("abhishek"); // this is not good readability is gone which should be always high

// https://tc39.es/ecma262/  // Original documentations

let name = "Amit"; // string
let age = 31; // number
let isLoggedIn = true; // boolean
let state; // undefined
let emptyValue = null; // it is empty but not undefined simple khali hai

// number ==> 2 to power 53
// bigint
// string ==> " "
// boolean ==> true/false
// null == a type / standalone value
// undefined ==> means we didn't assign a value yet
// symbol ==> unique

// object

console.log(typeof "Abhishek");
console.log(typeof age);
console.log(typeof null);
console.log(typeof undefined);
